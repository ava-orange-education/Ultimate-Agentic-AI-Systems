# MCP SDK with CLI extras
mcp[cli]>=1.24.0

# Google ADK and the Python Gemini client (carried over from Chapter 2)
google-adk>=1.32.0
google-genai>=1.0.0

# Loaded by every script for env-var management
python-dotenv>=1.0.0
